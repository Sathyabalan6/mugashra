@AGENTS.md

# Project Workflow & Memory Instructions
- **Git Discipline**: Every major change, bug fix, redesign pass, and feature must be cleanly committed and pushed to GitHub (`origin/main`).
- **Commit Messages**: Always use simple, concise, and clean commit messages (e.g. `fix: accessibility and styling`, `feat: date availability checker`, `update design system`).
